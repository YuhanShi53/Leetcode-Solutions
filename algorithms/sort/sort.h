#include <nl_types.h>

void BubbleSort(int* array, uint length, bool isDescend);
void InsertionSort(int* array, uint length, bool isDescend);
void SelectionSort(int* array, uint length, bool isDescend = false);
void ShellSort(int* array, uint length, bool isDescend = false);
