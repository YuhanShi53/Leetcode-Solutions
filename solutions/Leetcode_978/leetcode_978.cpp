#include <vector>

class Solution1
{
public:
    int maxTurbulenceSize(std::vector<int> &arr)
    {
        int max_size = 1;
        int local_size = 1;
        int prev_diff = arr.size() > 1 ? arr[1] - arr[0] : 1;
        for (int i = 1; i < arr.size(); i++)
        {
            
        }
    }
}